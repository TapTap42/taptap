 "use client";
import {useEffect,useState} from "react";
export default function TapClient({business}:{business:any}){const[rating,setRating]=useState(0),[message,setMessage]=useState(""),[sent,setSent]=useState(false);
useEffect(()=>{fetch("/api/tap",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({business_id:business.id})}).catch(()=>{})},[business.id]);
async function submit(){if(!rating)return;await fetch("/api/feedback",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({business_id:business.id,rating,message})});setSent(true)}
function google(){fetch("/api/google-click",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({business_id:business.id})}).catch(()=>{});window.location.href=business.google_review_url}
return <main style={{minHeight:"100vh",display:"grid",placeItems:"center",padding:22}}><section className="card" style={{width:"min(520px,100%)",textAlign:"center"}}><div style={{fontWeight:900,fontSize:22}}>{business.name}</div><div className="muted">La tua esperienza conta.</div><div className="stars">★★★★★</div><h1>Come ti sei trovato?</h1><div className="rating-buttons">{[1,2,3,4,5].map(n=><button key={n} className={rating===n?"active":""} onClick={()=>setRating(n)}>{n}</button>)}</div><textarea className="field" value={message} onChange={e=>setMessage(e.target.value)} placeholder="Feedback opzionale..."/>{!sent?<button className="primary" onClick={submit} disabled={!rating}>Invia feedback</button>:<div className="success">Feedback registrato. Grazie.</div>}<div style={{borderTop:"1px solid #e7ebef",marginTop:25,paddingTop:22}}><div className="muted" style={{fontSize:13}}>Vuoi condividere pubblicamente la tua esperienza?</div><button className="secondary" onClick={google}>Continua su Google →</button></div><div className="muted" style={{fontSize:10,marginTop:22}}>Powered by TapTap</div></section></main>}
