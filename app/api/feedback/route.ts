import {NextResponse} from "next/server";import {adminClient} from "@/lib/admin";
export async function POST(req:Request){try{const{business_id,rating,message}=await req.json();const r=Number(rating);if(!business_id||r<1||r>5)return NextResponse.json({error:"invalid data"},{status:400});const db=adminClient();await db.from("feedback").insert({business_id,rating:r,message:String(message||"").slice(0,1000)});await db.from("events").insert({business_id,event_type:"feedback"});return NextResponse.json({ok:true})}catch{return NextResponse.json({error:"server error"},{status:500})}}
