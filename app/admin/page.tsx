 "use client";
import{useState}from"react";
export default function Admin(){const[key,setKey]=useState(""),[data,setData]=useState<any>(),[err,setErr]=useState("");async function load(){const r=await fetch("/api/admin/stats",{headers:{authorization:"Bearer "+key}});const j=await r.json();if(!r.ok){setErr(j.error||"Errore");return}setData(j)}return <main className="admin wrap"><h1>TapTap Dashboard</h1><div className="card"><input className="field" value={key} onChange={e=>setKey(e.target.value)} placeholder="Admin key"/><button className="primary" onClick={load}>Carica dati</button></div>{err&&<p className="error">{err}</p>}{data&&<><div className="grid" style={{marginTop:20}}><div className="metric">Tap NFC<b>{data.taps}</b></div><div className="metric">Feedback<b>{data.feedbacks}</b></div><div className="metric">Click Google<b>{data.google_clicks}</b></div></div><div className="card" style={{marginTop:20}}><h2>Feedback recenti</h2><table className="table"><thead><tr><th>Attività</th><th>Rating</th><th>Messaggio</th></tr></thead><tbody>{data.recent.map((x:any)=><tr key={x.id}><td>{x.business_name}</td><td>{x.rating}/5</td><td>{x.message||"—"}</td></tr>)}</tbody></table></div></>}</main>}
